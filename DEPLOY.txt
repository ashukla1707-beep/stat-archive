STAT ARCHIVE — Production Release

Upload the CONTENTS of this folder to the repository root.

Required alongside index.html:
- assets/styles.css
- assets/app.js
- manifest.json
- sw.js
- icons/

If manifest.json, sw.js, or icons/ are not present in this package because they are
already in your repository, keep your existing current versions in the repository root.

After deployment, allow the service worker/site cache to update before judging changes.
